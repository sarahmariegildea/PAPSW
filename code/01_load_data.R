# read in data ------------------------------------------------------------

# use a csv dataset like this:
df <- readr::read_csv(here("'/Users/Sarah/Documents/The 2985 with Critera_DSM5_PTSD = 0 or 1.xlsx'"))

# or use an example dataset like penguins from palmerpenguins:
# use View to look aView(penguins)vt it in RStudio
View(penguins)vVView(penguins)view(penguins)vView(penguins)v